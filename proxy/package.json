{
  "name": "superdoc-ai-proxy",
  "version": "1.0.0",
  "description": "API proxy for SuperDoc AI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --env-file=.env server.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.24.3"
  },
  "engines": {
    "node": ">=18"
  }
}